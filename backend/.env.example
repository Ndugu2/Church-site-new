SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://localhost:5173
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DJANGO_LOG_LEVEL=INFO
API_REQUEST_LOG_LEVEL=INFO
# --- Vercel Postgres (auto-injected when you connect a Postgres store) ---
# POSTGRES_URL_NON_POOLING=  # Direct connection — used by Django (migrations + runtime)
# POSTGRES_URL=              # Pooled connection — fallback
# Leave all three unset locally to use SQLite instead.
DATABASE_URL=
